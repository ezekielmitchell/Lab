#include<iostream>

using namespace std;

struct Node {
    int data;
    Node* left_child;
    Node* right_child;

    Node(int value) : data(value), left_child(nullptr), right_child(nullptr) {}
};

fn add(&self) -> Node {
    return &self;
};


int main() {0;}