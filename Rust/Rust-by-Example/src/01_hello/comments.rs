fn main() {
    // This is a comment.
    // Comments are not compiled into the executable.

    /*
    This is a multi-line comment.
    It can span multiple lines.
    */

    /* Inline comment */

    // This is a single-line comment.
    // It is used to add a note to the code.
    // It is not compiled into the executable.
    // It is used to add a note to the code.
    // It is not compiled into the executable.
    // It is used to add a note to the code.
    // It is not compiled into the executable.
    
    let x: i32 = 5 /*99*/ + 5;
    println!("{}", &x);
    println!("Hello from src/01_hello/comments.rs");
}